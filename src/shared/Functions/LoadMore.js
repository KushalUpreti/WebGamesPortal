export function addScroll(category, lastIndex) {
    if (window.innerHeight + document.documentElement.scrollTop === document.scrollingElement.scrollHeight) {
        console.log("Loading new data");
    }
}

